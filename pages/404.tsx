import React from 'react';
import {Container, Text} from '@chakra-ui/react';

const NotFoundPage = () => (
	<Container>
		<Text fontSize="2xl" fontWeight="bold" textAlign="center">Page not found.</Text>
	</Container>
);

export default NotFoundPage;
